//problem 3 -- If -else condition
//to verify user's age whther eligible for vote or not ?
#include<iostream>
using namespace std;
int main(){
    int age;
    cin>> age;
    
    if(age>=18){
        if(age>=100){
            cout<<"Eligible for vote(But we are super senior citizen";
        }else{
            cout<<"Eligible for vote";
        }
    }else{
        if(age<0){
            cout<<"Enter a valid age number";
        }else{
            cout<<"Not eligible for vote";
        }
    }
    return 0;
}

//Problem-4 -- Ternary operator
//Find the greater number among 2

#include<iostream>
using namespace std;

int main(){
    int a,b;
    int max_number;
    
    cin>> a>>b;
    //ternary operator
    max_number=(a>b) ? a:b;
    
    cout<< max_number;
    return 0;
}
